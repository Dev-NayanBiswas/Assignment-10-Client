import { Outlet } from "react-router-dom"


function Registration() {
  return (
    <section>
        <section>
            <Outlet/>
        </section>
    </section>
  )
}

export default Registration